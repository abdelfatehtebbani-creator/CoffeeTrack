# Changelog

كل التغييرات المهمة على المشروع تُوثَّق هنا. التنسيق مستوحى من [Keep a Changelog](https://keepachangelog.com/).

## [1.0.0] — الإصدار الأول

### Added
- بنية مشروع Google Apps Script كاملة: `Code.gs`, `Setup.gs`, `SheetService.gs`, `Auth.gs`, `EntryOperations.gs`, `ReportsOperations.gs`.
- 3 صفحات HTML: `Login.html`, `Entry.html`, `Reports.html`، بتصميم عصري موحّد عبر `Style.html`.
- دعم ثنائي اللغة (عربي RTL / إنجليزي LTR) بنمط `data-ar`/`data-en` + زر تبديل لغة في كل صفحة.
- شعار AL AKER SWEETS مضمّن كـ Base64 داخل `LogoBase64.html` (بدون الحاجة لاستضافة خارجية).
- نظام مصادقة بسيط (username/password) مخزَّن في شيت `Users`، مع جلسات مؤقتة عبر `CacheService` (٦ ساعات).
- 3 أدوار مستخدمين: `Admin` (كل الصلاحيات)، `DataEntry` (صفحة الإدخال فقط)، `Accountant` (صفحة التقارير فقط).
- 5 نماذج إدخال بيانات تغطي دورة القهوة الكاملة:
  1. استلام حبوب من المورد (Coffee Beans Received from the Supplier)
  2. إرسال حبوب للتحميص (Coffee Beans Sent to the Roastery)
  3. استلام مطحون بعد التحميص (Ground Coffee Received from the Roastery) — مع حساب تلقائي لنسبة الهدر
  4. تعبئة القهوة (Coffee Under Processing / Packing) — تحويل كغ إلى أكياس 200غ مع حساب هدر التعبئة
  5. تسجيل منتج نهائي جاهز (Ready-Made Packed Coffee)
- تحقق صارم (Strict validation): كل عملية تُرفض إذا تجاوزت الرصيد المتوفر فعلياً في المرحلة السابقة.
- لوحة مؤشرات (KPI dashboard) في صفحة الإدخال تعرض الأرصدة الحالية لحظياً.
- 6 تقارير في صفحة التقارير:
  1. الكميات المستلمة من المورد حسب الصنف
  2. الكميات المرسلة للتحميص
  3. الكميات المستلمة بعد التحميص + نسبة الهدر
  4. الكمية المحمصة المتوفرة حالياً
  5. الكمية قيد التعبئة (نصف جاهزة) + نسبة هدر التعبئة
  6. الكمية الجاهزة (منتج نهائي)
- دالة `initializeSpreadsheet()` لإنشاء كل الشيتات + مستخدم Admin افتراضي بضغطة واحدة.
- سجل تدقيق (`AuditLog`) يسجّل كل عملية تسجيل دخول أو إدخال بيانات.
- توثيق كامل للمشروع: `README.md`, `CLAUDE.md`, `docs/Architecture.md`, `docs/Database.md`, `docs/API.md`, `docs/TODO.md`.

### Changed
- لا يوجد (إصدار أول).

### Added
- **تحقق جديد: "الرصيد عند المحمصة"** (`getAtRoasteryBalance_` في `SheetService.gs`) — يمنع الآن إدخال `Sent Quantity (kg)` في نموذج "استلام من التحميص" بقيمة أكبر من الكمية المتبقية فعلياً عند المحمصة (المرسلة ولم تُسوَّ بعد عبر أي استلام سابق). يدعم هذا بشكل صحيح حالات تجزئة الدفعة العائدة على عدة استلامات، أو دمج عدة إرسالات في استلام واحد مخلوط. يظهر هذا الرصيد كمؤشر KPI حي في صفحة الإدخال ("عند المحمصة الآن") وفي تقرير "الكميات المرسلة للتحميص". راجع `docs/Database.md` (قسم "مفهوم عند المحمصة الآن") للشرح الكامل مع الأمثلة.

### Added
- **فحص ذاتي لاكتشاف "القيمة الفاسدة" في `getAllReports()`** (`findUnserializableValue_` في `ReportsOperations.gs`): قبل إرجاع بيانات التقارير للعميل، يفحص الكود الآن الكائن بالكامل بحثاً عن أي قيمة `NaN`/`Infinity`/`undefined` في أي عمق، ويرمي خطأ صريحاً يذكر **المسار الدقيق** لتلك القيمة (مثال: `result.packingInProgress.rows[2].wasteKg`) بدل ترك المشكلة تظهر كـ "صفحة عالقة على Loading" أو "بيانات فارغة (null)" غامضة. هذا يحلّ فئة كاملة من الأخطاء الصامتة المعروفة في Apps Script (تحويل الاستجابة بالكامل لـ `null` دون أي رسالة عند وجود قيمة واحدة غير قابلة للتسلسل). راجع `CLAUDE.md` (قسم أسلوب الكود) للنمط الملزم عند إضافة أي دالة تُرجع بيانات مركّبة مستقبلاً.

### Fixed
- **🔴 خلل أمني حقيقي: تجاوز التحقق الصارم عبر نقرة مزدوجة (Race Condition)**: اكتُشف أن نقرتين شبه متزامنتين على زر الحفظ (شائع على الهاتف) في أي من نماذج الإدخال الخمسة يمكن أن تقرآ **نفس** الرصيد المتاح قبل أن يكتب أيّ منهما، فيتجاوز أحد الطلبين التحقق الصارم رغم أن المجموع الفعلي يتعدّى المتاح — وقد لوحظ ذلك فعلياً: كمية أُرسلت للتحميص تتجاوز المخزون المتوفر، مع ظهور رسالة خطأ صحيحة لأحد الطلبين، لكن بيانات الطلب الآخر كُتبت رغم ذلك. **الإصلاح**: كل دوال `submit*` الخمس في `EntryOperations.gs` تُنفَّذ الآن (من التحقق وحتى الكتابة) داخل قفل حصري ذرّي (`withLock_()` الجديدة في `SheetService.gs`، مبنية على `LockService.getScriptLock()`)، بالإضافة إلى تعطيل زر الحفظ في `Entry.html` أثناء الإرسال كطبقة وقاية إضافية في الواجهة. راجع `CLAUDE.md` §4.1 للقاعدة الملزمة الجديدة لأي دالة كتابة مستقبلية.

### Added
- **فلتر فترة زمنية في صفحة التقارير** (`من تاريخ` / `إلى تاريخ`): يسمح بعزل "الدفعة الحديثة من الإنتاج" عن أي رصيد افتتاحي/قديم مُدخَل بتاريخ سابق، بحيث تعكس نِسَب الهدر والإجماليات أداء الفترة المطلوبة فقط. طُبِّق على التقارير 1, 2, 3, 5, 6 (`ReportsOperations.gs` → `filterByDateRange_`, `dateOnly_`)؛ التقرير 4 (القهوة المحمصة المتوفرة) والأرصدة اللحظية الأخرى ("عند المحمصة الآن"، "أكياس قيد التعبئة") تبقى دائماً تعرض الرصيد الفعلي الحالي بلا فلترة، لأنها حقائق لحظية وليست مجاميع على فترة. راجع `docs/API.md` للتفاصيل الكاملة لكل دالة.

### Fixed
- **بطاقات KPI (الإحصائية) لا تتبدّل لغتها عند التبديل بين عربي/إنجليزي**: كانت `renderKpis_()` (`Entry.html`) و`renderAll_()` (`Reports.html`) تُبنى ديناميكياً بجافاسكربت مرة واحدة فقط عند التحميل، ولا تُعاد بناؤها عند `setLang()` (الذي يحدّث فقط عناصر `.i18n` الثابتة). **الإصلاح**: تخزين آخر بيانات مُستلمة (`lastBalances_`, `lastReportsData_`) وإعادة استدعاء دالة الرسم نفسها داخل `setLang()` بعد كل تبديل لغة.
- **السبب الجذري الحقيقي لمشكلة "صفحة التقارير عالقة / بيانات null من google.script.run"**: بعد استبعاد كل الاحتمالات الأخرى بالترتيب (NaN، صلاحيات OAuth، حجم البيانات)، تبيّن أن السبب هو كائنات `Date` حقيقية (عمود التاريخ من الشيت) تُرجَع كما هي ضمن كائنات كبيرة عبر `return` عادي — هذا يُسبب فشلاً صامتاً تاماً في جسر Apps Script الداخلي (يصل `null` للعميل بلا أي خطأ ظاهر، وسجل Executions يبقى "Finished"). أُثبت هذا باختبار حاسم: نفس البيانات، عند تحويلها لنص ورميها كـ`Error`، وصلت كاملة وسليمة؛ بينما `return` العادي لنفس الكائن (بكائنات `Date` غير محوَّلة) فشل. **الإصلاح**: `readSheetAsObjects_()` في `SheetService.gs` يحوّل الآن أي قيمة `Date` إلى نص ISO فور القراءة من الشيت، فلا تُرجَع كائنات `Date` خام من أي دالة عامة بعد الآن. راجع `CLAUDE.md` §8.4 للتفاصيل الكاملة وطريقة تشخيص أي حالة مشابهة مستقبلاً.
- ~~"صلاحية OAuth غير مُصرَّح بها" كسبب لمشكلة `null`~~ — **تشخيص أولي خاطئ تم استبعاده لاحقاً بالدليل** (الكتابة/القراءة عبر `SpreadsheetApp.openById()` كانتا تعملان بنجاح طوال الوقت). أُبقي `oauthScopes` في `appsscript.json` كإجراء احترازي غير ضار، لكن السبب الفعلي موثّق في البند أعلاه. راجع `CLAUDE.md` §8.3.
- **`Sheet not found: RawMaterial_Received`**: `SPREADSHEET_ID` في `Setup.gs` كان قد رجع لقيمته الافتراضية (`PASTE_YOUR_SPREADSHEET_ID_HERE`) بعد تحديث سابق، بينما بيانات المستخدم الفعلية موجودة في شيت "CoffeeTrack Database" منفصل عن الشيت المرتبط بمشروع Apps Script نفسه (Container-bound). ثُبِّت معرّف الشيت الصحيح للمستخدم (`1mwRfPsZXXEvrkBDCiPafiG7bqfAdKJL6ff4orW-7XSc`) بشكل دائم، وأُضيفت قاعدة صريحة في `CLAUDE.md` (§8.1) تمنع أي تحديث مستقبلي من الكتابة فوق هذه القيمة بالخطأ.
- **صفحة التقارير عالقة على "Loading reports..." بلا أي رسالة خطأ**: السبب الجذري هو نمط `Number(x || 0)` في دوال التجميع (`SheetService.gs` و`ReportsOperations.gs`) — إن وُجد نص غير رقمي بالخطأ في أي عمود كمية بالشيت، يتحوّل الحساب إلى `NaN` بصمت، مما يكسر تحويل استجابة `getAllReports()` بالكامل إلى JSON فيصل `null` للعميل بدل خطأ واضح (يظهر في الـ Console كـ `TypeError: Cannot read properties of null (reading 'rawReceived')`). **الإصلاح**: تغيير النمط إلى `(Number(x) || 0)` في كل دوال الجمع، بالإضافة إلى (1) تغليف `getAllReports` بـ `try/catch` يرمي خطأ نصياً واضحاً بدل الفشل الصامت، و(2) حماية دفاعية في `renderAll_` (`Reports.html`) تعرض رسالة واضحة للمستخدم إن وصلت بيانات فارغة/تالفة لأي سبب مستقبلي. راجع `CLAUDE.md` (قسم أسلوب الكود) للقاعدة الملزمة الجديدة.
- **صفحة بيضاء فارغة بعد تسجيل الدخول (blank white page)**: كانت كل الصفحات تبني رابط التنقّل بين الصفحات (`getBaseUrl_()`) من `window.location.href` في طرف العميل. لكن Apps Script HTML Service يعرض المحتوى داخل إطار معزول (iframe) على نطاق `googleusercontent.com` مختلف عن رابط `/exec` الحقيقي، فكان `window.location.href` يعطي رابط الإطار الداخلي، والتنقّل إليه مباشرة (`window.top.location.href = ...`) يفتح صفحة فارغة بدون سياق تنفيذ صحيح. **الحل**: `Code.gs` يحقن الرابط الحقيقي عبر `ScriptApp.getService().getUrl()` في متغيّر `baseUrl` يُقرأ داخل كل صفحة كـ `const BASE_URL = '<?!= baseUrl ?>';`. طُبِّق على `Login.html`, `Entry.html`, `Reports.html`. راجع `CLAUDE.md` §8.2 للتفاصيل الكاملة وقاعدة عدم تكرار هذا الخطأ مستقبلاً.
- **`SPREADSHEET_ID` مفقود لمشاريع clasp المستقلة**: أُضيف ثابت `SPREADSHEET_ID` في `Setup.gs` + منطق في `getSS_()` (`SheetService.gs`) يستخدم `SpreadsheetApp.openById(SPREADSHEET_ID)` إن كان محدَّداً، ويعود تلقائياً لـ `getActiveSpreadsheet()` فقط إذا كان المشروع Container-bound. السبب: `SpreadsheetApp.getActiveSpreadsheet()` يعمل فقط إن كان المشروع مرتبطاً مباشرة بشيت من داخله؛ مشاريع `clasp create` المستقلة تحتاج تحديد الـ ID صراحة، وإلا يفشل الكود بصمت أو بخطأ غامض. راجع README (قسم "ربط المشروع") لخطوات إيجاد الـ ID ووضعه.
- **`ReferenceError: ROLES is not defined`**: استُبدلت الثوابت `ENTRY_ROLES` (في `EntryOperations.gs`) و`REPORT_ROLES` (في `ReportsOperations.gs`) — وكانتا `const` على المستوى الأعلى تعتمدان على `ROLES` المعرَّف في `Setup.gs` — بدالتين محسوبتين وقت التنفيذ (`entryRoles_()`, `reportRoles_()`). السبب: Apps Script يحمّل الملفات أبجدياً، و`EntryOperations.gs`/`ReportsOperations.gs` (E/R) تُحمَّل قبل `Setup.gs` (S)، فكان `ROLES` غير معرَّف بعد عند وصول المحمل إليها. راجع `CLAUDE.md` §8 للتفاصيل والقاعدة العامة لتفادي هذا مستقبلاً.

### Planned
راجع [docs/TODO.md](docs/TODO.md) للخارطة الكاملة. أبرز النقاط:
- تشفير كلمات المرور (حالياً نص عادي في شيت `Users` — مقبول للنسخة الداخلية الأولى فقط).
- توحيد كود `setLang()`/i18n المكرر بين الصفحات الثلاث في ملف مشترك.
- دعم عدد ديناميكي من أصناف القهوة (حالياً صنفان ثابتان في `Config`).
- تصدير التقارير إلى PDF/Excel.
