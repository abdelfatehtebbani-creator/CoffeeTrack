# CLAUDE.md — تعليمات دائمة للتطوير المستقبلي

هذا الملف مرجع دائم لأي عمل مستقبلي على مشروع **Coffee Track**. اقرأه قبل أي تعديل.

## 1) البنية المعمارية (لا تُغيَّر بدون نقاش)
```
HTML (Login/Entry/Reports) --google.script.run--> EntryOperations.gs / ReportsOperations.gs / Auth.gs
                                                         │
                                                         ▼
                                                  SheetService.gs  ---> Google Sheets
                                                         ▲
                                                    Setup.gs (constants + init)
```
- **طبقة واحدة فقط** تتحدث مع `SpreadsheetApp` مباشرة: `SheetService.gs` (+ `Setup.gs` للتهيئة الأولى فقط). أي كود جديد يحتاج بيانات من الشيت يجب أن يمر عبر دوال `SheetService.gs`، لا يُستدعى `SpreadsheetApp.getActiveSpreadsheet()` من أي ملف آخر.
- **كل عملية كتابة تمر عبر `appendRow_()`** لضمان توليد ID موحّد وحقل `Timestamp`.
- **كل دالة تُستدعى من الواجهة (`google.script.run`) يجب أن تبدأ بالتحقق من الجلسة/الصلاحية** عبر `requireSession_()` أو `requireRole_()` من `Auth.gs`.

## 2) قواعد التسمية (Naming Conventions)
- الدوال **العامة** (تُستدعى من HTML عبر `google.script.run`): بدون شرطة سفلية، أسماء أفعال واضحة بالإنجليزية: `submitRawReceived`, `getAllReports`, `login`.
- الدوال **الخاصة/الداخلية** (لا تُستدعى من الواجهة أبداً): تنتهي بشرطة سفلية `_`: `requireSession_`, `appendRow_`, `sumField_`. هذا نمط Apps Script القياسي لإخفاء الدوال عن أي استدعاء خارجي محتمل.
- أسماء الشيتات ثابتة في `SHEETS` (كائن في `Setup.gs`) — **لا تكتب اسم شيت كنص حرفي (`'RawMaterial_Received'`) في أي مكان آخر**، استخدم `SHEETS.RAW_RECEIVED` دائماً.
- الأدوار ثابتة في `ROLES` — لا تكتب `'Admin'` كنص حرفي، استخدم `ROLES.ADMIN`.

## 3) كيف تتواصل الخدمات (Service Communication)
- **HTML → Server**: حصراً عبر `google.script.run.withSuccessHandler().withFailureHandler().functionName(...)`. لا يوجد REST API ولا `fetch()` للسيرفر.
- **كل استدعاء من الواجهة يرسل `token` كأول باراميتر** (باستثناء `login`). هذا التوكن يُحفظ في `sessionStorage` على المتصفح (وليس `localStorage`).
- **Server → Sheets**: حصراً عبر `SheetService.gs`.
- لا يوجد اتصال مباشر بين `EntryOperations.gs` و`ReportsOperations.gs`؛ كلاهما يعتمد فقط على `SheetService.gs`.
- **دالة عامة تُستخدم من الصفحتين معاً (Entry و Reports) بلا تمييز دور** (مثال: `getTodayActivity()`) **تعيش في `SheetService.gs`** مباشرة، وليست في `EntryOperations.gs` أو `ReportsOperations.gs` — هذا يحافظ على القاعدة أعلاه (لا اتصال مباشر بين الملفين) ويتفادى ازدواجية الكود. تحقّق الصلاحية لمثل هذه الدوال عبر `requireSession_()` فقط (أي دور)، وليس `entryRoles_()`/`reportRoles_()`.

## 4) الأنماط التصميمية المستخدمة (Design Patterns)
- **Repository-lite pattern**: `SheetService.gs` يعمل كطبقة وصول بيانات موحّدة (يشبه Repository)، يُخفي تفاصيل `Range`/`getValues` عن باقي الكود.
- **Balance-by-subtraction pattern**: لا يوجد عمود "رصيد" مخزَّن في أي شيت. كل رصيد (مخزون خام، محمص، أكياس قيد التعبئة) يُحسب لحظياً بطرح المخرجات من المدخلات (انظر دوال `sum*_` و`get*Balance_` في `SheetService.gs`). **هذا مقصود**: يمنع تعارض البيانات (data drift) بين عمود مخزَّن وقيمة محسوبة. **لا تُضف عمود "Balance" ثابت في أي شيت إلا إذا كان الهدف Cache صريح مع إعادة حساب دورية موثّقة.**
- **Strict validation pattern**: كل دالة إدخال (`submit*` في `EntryOperations.gs`) تتحقق من الرصيد المتوفر في المرحلة السابقة **قبل** الكتابة، وترفض العملية كلياً إن كانت الكمية أكبر (قرار مشروع مؤكد من المستخدم، وليس افتراضاً تقنياً — **لا تُخفّفه إلى "تحذير فقط" بدون تأكيد صريح من المستخدم**).
- **i18n بدون مكتبة خارجية**: كل نص قابل للترجمة عنصر `<span class="i18n" data-ar="..." data-en="...">`, وتبديل اللغة عبر `setLang()` في كل صفحة (مكرر حالياً في 3 ملفات — مرشح جيد لإعادة الهيكلة، انظر `docs/TODO.md`).
- **⚠️ محتوى ديناميكي وتبديل اللغة**: أي محتوى يُبنى بجافاسكربت (بطاقات KPI، صفوف الجداول، أي `innerHTML` يحتوي نصاً بلغتين) **ليس** عنصر `.i18n` ثابتاً، لذا `setLang()` لا يُحدّثه تلقائياً. **يجب** تخزين آخر بيانات مستلمة من الخادم في متغيّر (مثال: `lastBalances_` في `Entry.html`, `lastReportsData_` في `Reports.html`) واستدعاء دالة إعادة الرسم نفسها من داخل `setLang()` بعد تحديث عناصر `.i18n`. نسيان هذه الخطوة يُنتج خللاً صامتاً غير واضح (البطاقات تبقى بلغة التحميل الأولى رغم تبديل اللغة) — تم اكتشافه وإصلاحه فعلياً في هذا المشروع.

## 4.1) القفل الذري (LockService) — قاعدة إلزامية لأي عملية "تحقق ثم اكتب"
اكتُشف خلل أمني حقيقي في الإنتاج: نقرة مزدوجة على زر الحفظ (شائعة على الهاتف) أرسلت طلبين شبه متزامنين لنفس دالة `submit*`؛ كلاهما قرأ **نفس** الرصيد المتوفر قبل أن يكتب أيّ منهما، فتجاوز أحدهما التحقق الصارم رغم أن المجموع الفعلي يتعدّى المتاح.

**الحل الثابت**: كل دالة `submit*` في `EntryOperations.gs` تلفّ منطقها بالكامل (من قراءة الرصيد وحتى `appendRow_`) داخل `withLock_(function(){ ... })` المعرَّفة في `SheetService.gs` — قفل حصري على مستوى المشروع (`LockService.getScriptLock()`) يضمن تنفيذ "تحقق ثم اكتب" كوحدة ذرّية واحدة، بغض النظر عن عدد الطلبات المتزامنة.

**قاعدة إلزامية لأي دالة كتابة جديدة**: أي دالة تقرأ رصيداً (عبر أي دالة `get*Balance_`/`sum*_`) ثم تكتب صفاً بناءً على نتيجة ذلك التحقق، **يجب** أن تُغلَّف بـ`withLock_()` — بدون استثناء، بغض النظر عن مدى "بساطة" العملية أو "عدم احتمالية" حدوث تزامن. طبقة الوقاية الإضافية في العميل (تعطيل الزر أثناء الإرسال في `Entry.html`) مفيدة لتجربة المستخدم لكنها **ليست بديلاً** عن القفل على الخادم — يمكن تجاوزها بسهولة (تحديث الصفحة، تبويبان مفتوحان، طلب مباشر عبر Console).

## 4.2) قرار تصميمي مهم: "الكمية المرسلة" في "استلام من التحميص" تُقدَّر تلقائياً من متوسط الهدر
هذا القسم يوثّق **تطوراً بثلاث مراحل** لنفس الحقل، حتى لا تتكرر الأخطاء السابقة عند أي تعديل مستقبلي:

**المرحلة 1 (الأصلية)**: `SentQuantityKg` يُدخَل يدوياً من المستخدم لكل صف استلام. المشكلة: تخمين غير موثوق (المحمصة قد تُجزّئ/تخلط الدفعات، فلا يمكن معرفة الكمية الحقيقية المقابلة).

**المرحلة 2 (جُرِّبت وثبت خطؤها)**: حُذف الحقل تماماً، و`getAtRoasteryBalance_()` أصبحت تقارن إجمالي الشيتين مباشرة (`Σ Sent - Σ Received.ReceivedQuantityKg`). **هذا كان خطأً فعلياً ظهر في الإنتاج**: الفرق بين الإجماليين يخلط بين "خام لا يزال فعلياً عند المحمصة" و"هدر تراكمي من دفعات اكتملت بالفعل" — فمثلاً إرسال 100كغ واستلامها بالكامل 88كغ (هدر طبيعي 12%) يجب أن يُصفّر رصيد "عند المحمصة"، لكن تلك الصيغة كانت تُبقيه عالقاً عند 12كغ وهمية للأبد حتى بعد اكتمال الاستلام فعلياً.

**المرحلة 3 (الحالية)**: `SentQuantityKg` يُحسب **تلقائياً** (وليس يدوياً، وليس محذوفاً) عبر:
```
الكمية المرسلة المقدَّرة = الكمية المستلمة ÷ (1 − متوسط نسبة الهدر)
```
حيث "متوسط نسبة الهدر" = `Config.AverageRoastingWastePercent` (رقم يضبطه المستخدم يدوياً بناءً على خبرته التشغيلية، ويُحدَّثه دورياً ليطابق الواقع — راجع `docs/Database.md`).

**التبعات المعمارية للتصميم النهائي:**
- `submitReceivedFromRoastery()` يطلب `receivedQuantityKg` فقط من المستخدم (لا إدخال يدوي لـ`sentQuantityKg`)، لكنه **يكتب** `SentQuantityKg`, `WasteKg`, `WastePercent` تلقائياً في الشيت (محسوبة، وليست فارغة).
- `getAtRoasteryBalance_()` = `sumSentToRoastery_() - sumReceivedFromRoasterySentField_()` — عادت لنفس صيغة المرحلة 1 الأصلية، لكن العمود الذي تعتمد عليه (`SentQuantityKg`) أصبح **محسوباً تلقائياً وموثوقاً** بدل تخمين يدوي. هذا يُصفّر الرصيد بشكل صحيح تلقائياً مع اكتمال كل دفعة (يحل مشكلة المرحلة 2)، مع بقاء تجربة المستخدم بسيطة (يُدخل رقماً واحداً فقط).
- ⚠️ **لا تُعِد صيغة المرحلة 2** (`Σ Sent - Σ Received.ReceivedQuantityKg` مباشرة) **بدون نقاش صريح** — ثبت خطؤها فعلياً في الإنتاج، والمثال أعلاه يوضّح السبب بدقة.
- **نسبة الهدر الإجمالية الحقيقية في التقارير تبقى مستقلة تماماً عن هذا التقدير**: `reportReceivedFromRoastery()` يحسبها دائماً من مقارنة مباشرة بين إجمالي شيت `Sent_to_Roastery` الفعلي وإجمالي `Received_from_Roastery.ReceivedQuantityKg` الفعلي — دقيقة 100% بغض النظر عن دقة `AverageRoastingWastePercent` المُعتمَد. أما `SentQuantityKg`/`WasteKg`/`WastePercent` **لكل صف** في نفس الشيت فهي تقديرية بالتعريف (تُساوي دائماً متوسط الهدر المُعتمَد بالضبط، لأنها مبنية عليه رياضياً) — لا تعرضها في أي مكان دون توضيح أنها "تقديرية" بصريح العبارة (كما في `Reports.html` حالياً).
- إن أضاف المستخدم مستقبلاً آلية لتحديد الهدر الفعلي الدقيق لكل دفعة (بدل الاعتماد على المتوسط)، فالمكان الصحيح للتعديل هو حساب `estimatedSent` داخل `submitReceivedFromRoastery()` فقط — بقية المعمارية (القفل، التحقق الصارم، التقارير) تبقى كما هي.

## 5) قواعد لا يجب كسرها أبداً
1. **لا تكتب بيانات مباشرة بدون المرور بـ `appendRow_()`** — لضمان ID + Timestamp موحّدين.
2. **لا تُزل التحقق الصارم من الأرصدة** في `EntryOperations.gs` دون تأكيد صريح من المستخدم (تم اختيار "منع تماماً" وليس "تحذير فقط" بناءً على قرار موثّق).
3. **لا تُخزّن كلمات المرور كنص عادي في بيئة إنتاج فعلية** — النظام الحالي (Password عادي في شيت `Users`) مقبول فقط للنسخة الأولى/الداخلية؛ أي تحسين أمني (hashing) يجب أن يُذكر في `docs/TODO.md` وليس أن يُنفَّذ بصمت لأنه يغيّر شكل بيانات شيت `Users` الحالية (breaking change).
4. **حافظ على التوافق مع الإصدارات السابقة** (backward compatibility) لأي تعديل على أعمدة الشيتات — إضافة عمود جديد في نهاية الصف مقبولة، **حذف أو إعادة ترتيب عمود موجود يكسر كل الصفوف السابقة ويتطلب تأكيداً صريحاً**.
5. **لا تحذف أي دالة عامة (public function) مستخدمة من HTML بدون التأكد من عدم استدعائها في أي من الملفات الثلاثة** (`Login.html`, `Entry.html`, `Reports.html`) وتحديث `docs/API.md` وفق ذلك.
6. **لا تُدخل مكتبات خارجية (npm/CDN) في ملفات `.gs`** — Apps Script لا يدعم `require`/`import` من خارج المشروع؛ فقط `<script src="https://...">` مسموح داخل HTML، وهذا غير مستخدم حالياً عمداً (السرعة + العمل دون اتصال بمصادر خارجية بطيئة).

## 6) أسلوب الكود المفضّل (Coding Style)
- JavaScript بأسلوب ES6 القياسي المدعوم في V8 runtime لـ Apps Script (`const`/`let`, arrow functions, template literals, `Array.map/reduce/filter`).
- كل رقم مالي/وزني يُقرَّب بـ `Math.round(x * 1000) / 1000` (3 خانات عشرية للكغ) أو `* 100) / 100` للنسب المئوية — **حافظ على هذا التقريب الموحّد** حتى لا تختلف الأرقام المعروضة بين التقارير.
- رسائل الأخطاء والنجاح **دائماً ثنائية اللغة** بصيغة: `'نص عربي / English text'` — هذا نمط ثابت في كل `EntryOperations.gs`/`Auth.gs`، حافظ عليه في أي دالة جديدة.
- التعليقات التوضيحية أعلى كل دالة عامة (JSDoc-lite) موجودة، استمر عليها لأي دالة جديدة تُستدعى من HTML.
- **تحويل الأرقام الآمن (إلزامي)**: عند تحويل قيمة من الشيت **أو من `Config`** لرقم مع قيمة افتراضية، اكتب دائماً `(Number(x) || 0)` — **وليس** `Number(x || 0)`. الفرق حاسم: الصيغة الثانية تُحوّل النص غير الرقمي إلى `NaN` بصمت (لأن `x || 0` يُبقي `x` كما هو إن كان "صحيحاً منطقياً" حتى لو نصاً غير رقمي)، و`NaN` قد يكسر تحويل استجابة `google.script.run` بالكامل إلى JSON فيصل `null` للعميل بدل رسالة خطأ واضحة. ⚠️ **هذا الخطأ تكرر فعلياً** في `getCurrentBalances()`/`submitReceivedFromRoastery()`/`submitPackingProcess()` عند إضافة قراءات `Config` جديدة (`AverageRoastingWastePercent`, `BagSizeKg`) رغم توثيق هذه القاعدة مسبقاً — أي قراءة جديدة من `Config` عبر `getConfigMap_()` معرَّضة لنفس الخطر بالضبط، وليست فقط القيم القادمة من صفوف الشيتات. راجع كل الدوال في `SheetService.gs` (`sum*_`) و`ReportsOperations.gs` (`sumField_`, `groupSum_`) كمرجع للنمط الصحيح.
- **الفحص الذاتي قبل إرجاع أي استجابة كبيرة للعميل**: `google.script.run` يحوّل الاستجابة بالكامل إلى `null` بصمت (بدون أي خطأ) إن وُجدت قيمة واحدة فقط في أي عمق من الكائن المُرجَع تكون `NaN`/`Infinity`/`undefined`/دالة. `getAllReports()` في `ReportsOperations.gs` يطبّق نمطاً دفاعياً ثابتاً لهذا: يبني الكائن الكامل، ثم يفحصه بالكامل عبر `findUnserializableValue_()` (فحص متكرر بالعمق) قبل إرجاعه، ويرمي خطأ صريحاً يذكر المسار الدقيق للقيمة الفاسدة إن وُجدت. **طبّق نفس النمط على أي دالة عامة جديدة تُرجع كائناً مركّباً كبيراً** (مصفوفات صفوف من الشيت، تجميعات متعددة) بدل الاكتفاء بـ `try/catch` عادي — لأن `try/catch` وحده لا يلتقط هذا النوع من الفشل الصامت (فهو ليس استثناءً أصلاً، بل قيمة إرجاع "سليمة" تحتوي خللاً بنيوياً).

## 7) اعتبارات واجهة RTL العربية
- `<body dir="rtl">` هو الافتراضي؛ `setLang('en')` يبدّل `document.body.dir` إلى `'ltr'` ديناميكياً — **لا تفترض اتجاهاً ثابتاً في أي CSS جديد**، استخدم خصائص منطقية (`inset-inline-start`, `margin-inline-*`) بدلاً من `left`/`right` الصريحة (كما هو مطبّق فعلاً في `.kpi` بـ `border-inline-start`).
- الخط العربي `Cairo` والإنجليزي `Poppins` محمّلان معاً من Google Fonts؛ أي نص جديد يجب أن يكون داخل عنصر `.i18n` بسمتي `data-ar`/`data-en` وليس نصاً ثابتاً بلغة واحدة.
- الأرقام (كميات، تواريخ) تبقى بصيغة غربية (Latin numerals) في كلا اللغتين حالياً — لم يُطلب Eastern Arabic numerals، لا تُغيّر هذا بدون طلب صريح.

## 8) قيود Google Apps Script التي يجب مراعاتها

### ⚠️ ترتيب تحميل الملفات أبجدي (Alphabetical file load order)
Apps Script يدمج كل ملفات `.gs` في نطاق عام واحد، لكنه يُحمّلها ويُنفّذ أي كود على **المستوى الأعلى (top-level)** فيها بترتيب **أبجدي حسب اسم الملف** — وليس حسب ترتيب الاعتماد (dependency order). هذا يعني:
- **لا تكتب أبداً `const X = SOMETHING_FROM_ANOTHER_FILE.prop;` على المستوى الأعلى لملف**، لأن الملف الذي يحتوي `SOMETHING_FROM_ANOTHER_FILE` قد يُحمَّل لاحقاً أبجدياً فيرمي `ReferenceError`. هذا ما حدث فعلياً: `EntryOperations.gs` (E) كان يحاول قراءة `ROLES` (المعرَّف في `Setup.gs`, S) عند التحميل، و`E` تسبق `S` أبجدياً.
- **الحل الثابت في هذا المشروع**: أي قيمة تعتمد على ثابت من ملف آخر (`ROLES`, `SHEETS`, إلخ) يجب أن تُحسَب **داخل دالة (lazy)** وليس كـ `const` في أعلى الملف. مثال: `entryRoles_()` و`reportRoles_()` بدلاً من `ENTRY_ROLES`/`REPORT_ROLES` كثوابت جاهزة.
- الثوابت الآمنة على المستوى الأعلى هي فقط تلك **المستقلة تماماً عن أي ملف آخر** (مثل `SESSION_DURATION_SEC` في `Auth.gs`)، أو المُعرَّفة في `Setup.gs` نفسه (`SHEETS`, `ROLES`, `BAG_SIZE_KG`) لأنها لا تعتمد على شيء خارجي.
- عند إضافة أي ثابت جديد يعتمد على ملف آخر، اتبع نمط `entryRoles_()`/`reportRoles_()`: دالة صغيرة تُستدعى وقت التنفيذ الفعلي، لا وقت تحميل الملف.

- **لا Session حقيقية من السيرفر**: الحل الحالي (`CacheService` + توكن UUID، صلاحية 6 ساعات) هو محاكاة يدوية — أي إعادة تصميم مصادقة يجب أن يحافظ على هذا المبدأ (توكن يُرسَل من العميل مع كل نداء) لأن Apps Script HTML Service عديم الحالة (stateless).
- **حد وقت التنفيذ**: 6 دقائق لكل تنفيذ (Consumer) — دوال القراءة الحالية (`readSheetAsObjects_`) تقرأ الشيت كاملاً بنداء واحد (`getRange().getValues()`) عمداً لتفادي نداءات متكررة بطيئة؛ **لا تستبدلها بحلقة `for` تقرأ خلية خلية**.
- **`CacheService` له حد حجم للقيمة المخزَّنة (~100KB)** — الجلسة الحالية تخزّن فقط username/role/fullName، لا تُضخّم هذا الكائن.
- **لا قاعدة بيانات علائقية حقيقية**: لا مفاتيح خارجية (Foreign Keys) ولا Transactions — الاتساق بين الشيتات مسؤولية الكود (التحقق الصارم في `EntryOperations.gs`) وليس مضموناً من المنصة. أي عملية تكتب لأكثر من شيت في نفس الوقت (لا يوجد حالياً) يجب أن تتحمّل احتمال فشل جزئي.
- **حد حجم ملف HTML/Script**: `LogoBase64.html` (~35KB نص) مقبول ضمن الحدود؛ لا تُضف صوراً كبيرة بنفس الطريقة (Base64) — استخدم Google Drive + `DriveApp` مع رابط عام إن احتجت صوراً إضافية أكبر.

## 8.1) SPREADSHEET_ID — قاعدة إلزامية لأي بيئة clasp
- `getSS_()` في `SheetService.gs` هو **المكان الوحيد** الذي يقرر أي Spreadsheet يُستخدم. لا تستدعِ `SpreadsheetApp.getActiveSpreadsheet()` أو `SpreadsheetApp.openById()` من أي ملف آخر — استخدم `getSS_()` دائماً.
- `SPREADSHEET_ID` في `Setup.gs` هو مصدر الحقيقة الوحيد لمعرّف الشيت. لا تُثبّت (hardcode) معرّف شيت في أي مكان آخر بالكود.
- عند تشغيل `initializeSpreadsheet()` أول مرة على مشروع clasp مستقل بدون تعيين `SPREADSHEET_ID`، ستحصل على خطأ واضح ثنائي اللغة بدل فشل صامت — حافظ على هذا السلوك (fail loudly) عند أي تعديل مستقبلي على `getSS_()`.
- ⚠️ **لا تستبدل أبداً القيمة الحالية لـ `SPREADSHEET_ID` بالقيمة الافتراضية `'PASTE_YOUR_SPREADSHEET_ID_HERE'` عند تسليم/دمج أي تحديث مستقبلي على `Setup.gs`.** حدث هذا فعلاً كخطأ حقيقي في هذا المشروع (Sheet not found بسبب رجوع القيمة للافتراضي بعد تحديث)، وسبَّب فشل كل التقارير. مشروع هذا المستخدم تحديداً يستخدم شيت بيانات (Database) **منفصل** عن الشيت الذي يستضيف مشروع Apps Script نفسه (Container-bound) — هذا إعداد سليم ومقصود، وليس خطأ يجب "تصحيحه".
- القيمة الحالية الصحيحة لهذا المشروع: `1mwRfPsZXXEvrkBDCiPafiG7bqfAdKJL6ff4orW-7XSc` (شيت "CoffeeTrack Database"). أي تعديل مستقبلي على `Setup.gs` يجب أن يُبقي هذا السطر كما هو ما لم يطلب المستخدم صراحة تغييره.

## 8.2) رابط /exec الحقيقي — لا تعتمد أبداً على window.location في العميل
واجهة Apps Script HTML Service تُعرض للمستخدم داخل **إطار معزول (sandboxed iframe)** على نطاق `googleusercontent.com`، مختلف تماماً عن نطاق `/exec` الظاهر في شريط العنوان. النتيجة:
- `window.location.href` من داخل أي صفحة `.html` **يعطي رابط الإطار الداخلي**، وليس رابط `/exec` الحقيقي.
- **لا تحاول أبداً بناء رابط التنقّل (`window.top.location.href = ...`) من `window.location` في العميل** — هذا يسبب انتقال المتصفح فعلياً إلى رابط الإطار الداخلي مباشرة، مما ينتج **صفحة بيضاء فارغة** (تم تشخيص هذا فعلياً وإصلاحه في هذا المشروع).
- **الحل الثابت المعتمد**: الخادم (`Code.gs` → `doGet`) يحقن الرابط الصحيح عبر `ScriptApp.getService().getUrl()` في متغيّر `template.baseUrl`، وكل صفحة HTML تقرأه كـ:
  ```js
  const BASE_URL = '<?!= baseUrl ?>';
  ```
  ثم تستخدم `BASE_URL` حصراً لكل تنقّل (`window.top.location.href = BASE_URL + '?page=...'`).
- أي صفحة `.html` جديدة تحتاج التنقّل بين الصفحات **يجب** أن تتبع نفس النمط (`template.xxx = ...` في `Code.gs` + `<?!= xxx ?>` في الصفحة)، لا تُعِد اختراع طريقة قراءة الرابط من طرف العميل.

## 8.3) ملاحظة تصحيحية: الصلاحيات (OAuth) لم تكن السبب الحقيقي
تم افتراض في البداية أن الوصول لشيت خارجي عبر `SpreadsheetApp.openById()` قد يحتاج إعادة تفويض صلاحيات، وأُضيف `oauthScopes` صراحة في `appsscript.json` كإجراء احترازي (لا ضرر منه، ويبقى موجوداً كتوثيق واضح للصلاحية المطلوبة). **لكن التشخيص الفعلي أثبت أن هذا لم يكن السبب** — الكتابة والقراءة عبر `openById()` كانتا تعملان بنجاح طوال الوقت. السبب الحقيقي موثّق في §8.4 التالي (كائنات `Date`). اترك هذا القسم كتذكير: لا تفترض تلقائياً أن أي `null` غامض من `google.script.run` سببه صلاحيات — تحقق فعلياً أولاً (راجع اختبار `throw` الموصوف في §8.4).

## 8.4) كائنات Date تكسر google.script.run بصمت — قاعدة حاسمة (تم تشخيصها فعلياً)
هذا أخطر خلل صامت واجهناه في هذا المشروع، وأخذ تشخيصه وقتاً طويلاً لأن **كل الأعراض ضلّلت نحو أسباب أخرى** (صلاحيات، بيانات فاسدة، حجم الاستجابة) قبل الوصول للسبب الحقيقي:

- `readSheetAsObjects_()` في `SheetService.gs` يقرأ عمود التاريخ من الشيت، فتصله قيمة من نوع **`Date` حقيقي** (وليس نصاً) — هذا سلوك طبيعي لـ`getValues()` مع أي عمود منسَّق كتاريخ.
- عند إرجاع كائن يحتوي هذه القيم (حتى لو متداخلة داخل مصفوفات كبيرة) عبر `return` عادي من دالة تُستدعى بـ`google.script.run`، **يفشل جسر Apps Script الداخلي بصمت تماماً** ويُرجع `null` للعميل — **بدون أي رسالة خطأ، وبدون فشل في سجل Executions (يبقى "Finished")**. هذا مختلف تماماً عن NaN/undefined (اللذين تناولهما `findUnserializableValue_` في §8 وفي `ReportsOperations.gs` — لكن الفحص هناك يتجاهل كائنات `Date` عمداً معتبراً إياها "مدعومة"، وهو صحيح نظرياً حسب توثيق غوغل، لكن غير صحيح عملياً في هذا النوع من الحمولات الكبيرة).
- **الدليل القاطع الذي كشف هذا**: نفس الكائن بالضبط، عند تحويله يدوياً إلى نص عبر `JSON.stringify()` ثم إرساله كرسالة `Error` (`throw`)، وصل **كاملاً وسليماً** للعميل. بينما إرجاعه طبيعياً (`return`) بدون تحويل مسبق يُنتج `null`. هذا يعزل المشكلة بدقة إلى: **مسار التسلسل الداخلي لكائنات `Date` في القناة الناجحة (success channel) تحديداً**.

### القاعدة الملزمة
- **`readSheetAsObjects_()` يحوّل الآن كل قيمة من نوع `Date` إلى نص `'yyyy-MM-dd'` بتوقيت المشروع (`Utilities.formatDate(v, Session.getScriptTimeZone(), 'yyyy-MM-dd')`) فور القراءة من الشيت** — هذا يعني أن أي كود يستهلك نتيجة هذه الدالة (`EntryOperations.gs`, `ReportsOperations.gs`) يستلم دائماً نصوصاً، لا كائنات `Date` أبداً. **لا تُرجِع كائن `Date` خام أبداً من أي دالة عامة تُستدعى عبر `google.script.run`** — حوّله لنص أولاً بغض النظر عن حجم البيانات أو تعقيدها.
- ⚠️ **لا تستخدم `Date.prototype.toISOString()` لهذا التحويل!** هذا خلل حقيقي ظهر في الإنتاج (كل التواريخ في التقارير تأخرت يوماً كاملاً عن الشيت الفعلي): `toISOString()` يحوّل دائماً لـ**UTC**، بينما تاريخ الشيت مخزَّن كمنتصف ليل **بتوقيت المشروع** (Asia/Qatar = UTC+3) — فيُرجع `toISOString()` قيمة اليوم السابق الساعة 21:00 (مثال: `2026-06-29` تصبح `"2026-06-28T21:00:00.000Z"`). استخدم `Utilities.formatDate(v, Session.getScriptTimeZone(), 'yyyy-MM-dd')` دائماً بدلاً منها لأي عمود تاريخ تقويمي (لا وقت).
- إن أضفت مستقبلاً أي مصدر بيانات جديد يقرأ من الشيت مباشرة (بدل المرور بـ`readSheetAsObjects_`)، طبّق نفس التحويل يدوياً.
- عند تشخيص أي حالة "استجابة `null` غامضة بلا خطأ" مستقبلاً: **جرّب دائماً هذا الاختبار أولاً** — `throw new Error(JSON.stringify(result))` مؤقتاً بدل `return result`. إن وصلت البيانات كاملة عبر رسالة الخطأ بينما تفشل عبر `return` العادي، فالسبب شبه مؤكد هو كائن `Date` (أو نوع مشابه غير قابل للتسلسل عبر القناة الناجحة تحديداً) في مكان ما بالبيانات.

## 9) افتراضات هيكل الشيت (راجع docs/Database.md للتفاصيل الكاملة)
- الصف 1 في كل شيت = رؤوس أعمدة (Headers)، ولا يجوز حذفه أو تغيير ترتيبه.
- عمود `ID` دائماً أول عمود، وعمود `Timestamp` دائماً آخر عمود — أي عمود جديد يُضاف **بينهما**، وليس بعد `Timestamp`.
- `Config` هو مصدر الحقيقة الوحيد لحجم الكيس (`BagSizeKg`) وأصناف القهوة (`CoffeeType1/2`) — **لا تُثبّت (hardcode) هذه القيم في أي دالة**؛ استخدم `getConfigMap_()`.

## 10) أفضل الممارسات عند تعديل المشروع
- أي تعديل على شكل بيانات شيت موجود (إضافة/حذف عمود) ⇒ حدّث `docs/Database.md` وأضف سطراً في `CHANGELOG.md` تحت `Changed`.
- أي دالة عامة جديدة تُستدعى من HTML ⇒ وثّقها في `docs/API.md` بنفس القالب المستخدم حالياً (Purpose / Parameters / Return / Errors / Called from).
- **حافظ دوماً على التوافق العكسي** ما لم يُطلب خلاف ذلك صراحة، و**لا تحذف وظيفة قائمة دون تأكيد صريح من المستخدم** — هذه قاعدة صارمة غير قابلة للتفاوض في هذا المشروع.
- قبل أي Refactor كبير (مثل توحيد `setLang()` في ملف واحد مشترك)، تأكد من عدم كسر أي من الصفحات الثلاث، واختبر يدوياً بعد `clasp push` + `New deployment`.
