# CLAUDE.md — تعليمات دائمة للتطوير المستقبلي

هذا الملف مرجع دائم لأي عمل مستقبلي على مشروع **Coffee Track**. اقرأه قبل أي تعديل.

## 1) البنية المعمارية (لا تُغيَّر بدون نقاش)
```
HTML (Login/Entry/Reports) --google.script.run--> EntryOperations.gs / ReportsOperations.gs / Auth.gs
                                                         │
                                                         ▼
                                                  SheetService.gs  ---> Google Sheets
                                                         ▲
                                                    Setup.gs (constants + init)
```
- **طبقة واحدة فقط** تتحدث مع `SpreadsheetApp` مباشرة: `SheetService.gs` (+ `Setup.gs` للتهيئة الأولى فقط). أي كود جديد يحتاج بيانات من الشيت يجب أن يمر عبر دوال `SheetService.gs`، لا يُستدعى `SpreadsheetApp.getActiveSpreadsheet()` من أي ملف آخر.
- **كل عملية كتابة تمر عبر `appendRow_()`** لضمان توليد ID موحّد وحقل `Timestamp`.
- **كل دالة تُستدعى من الواجهة (`google.script.run`) يجب أن تبدأ بالتحقق من الجلسة/الصلاحية** عبر `requireSession_()` أو `requireRole_()` من `Auth.gs`.

## 2) قواعد التسمية (Naming Conventions)
- الدوال **العامة** (تُستدعى من HTML عبر `google.script.run`): بدون شرطة سفلية، أسماء أفعال واضحة بالإنجليزية: `submitRawReceived`, `getAllReports`, `login`.
- الدوال **الخاصة/الداخلية** (لا تُستدعى من الواجهة أبداً): تنتهي بشرطة سفلية `_`: `requireSession_`, `appendRow_`, `sumField_`. هذا نمط Apps Script القياسي لإخفاء الدوال عن أي استدعاء خارجي محتمل.
- أسماء الشيتات ثابتة في `SHEETS` (كائن في `Setup.gs`) — **لا تكتب اسم شيت كنص حرفي (`'RawMaterial_Received'`) في أي مكان آخر**، استخدم `SHEETS.RAW_RECEIVED` دائماً.
- الأدوار ثابتة في `ROLES` — لا تكتب `'Admin'` كنص حرفي، استخدم `ROLES.ADMIN`.

## 3) كيف تتواصل الخدمات (Service Communication)
- **HTML → Server**: حصراً عبر `google.script.run.withSuccessHandler().withFailureHandler().functionName(...)`. لا يوجد REST API ولا `fetch()` للسيرفر.
- **كل استدعاء من الواجهة يرسل `token` كأول باراميتر** (باستثناء `login`). هذا التوكن يُحفظ في `sessionStorage` على المتصفح (وليس `localStorage`).
- **Server → Sheets**: حصراً عبر `SheetService.gs`.
- لا يوجد اتصال مباشر بين `EntryOperations.gs` و`ReportsOperations.gs`؛ كلاهما يعتمد فقط على `SheetService.gs`.

## 4) الأنماط التصميمية المستخدمة (Design Patterns)
- **Repository-lite pattern**: `SheetService.gs` يعمل كطبقة وصول بيانات موحّدة (يشبه Repository)، يُخفي تفاصيل `Range`/`getValues` عن باقي الكود.
- **Balance-by-subtraction pattern**: لا يوجد عمود "رصيد" مخزَّن في أي شيت. كل رصيد (مخزون خام، محمص، أكياس قيد التعبئة) يُحسب لحظياً بطرح المخرجات من المدخلات (انظر دوال `sum*_` و`get*Balance_` في `SheetService.gs`). **هذا مقصود**: يمنع تعارض البيانات (data drift) بين عمود مخزَّن وقيمة محسوبة. **لا تُضف عمود "Balance" ثابت في أي شيت إلا إذا كان الهدف Cache صريح مع إعادة حساب دورية موثّقة.**
- **Strict validation pattern**: كل دالة إدخال (`submit*` في `EntryOperations.gs`) تتحقق من الرصيد المتوفر في المرحلة السابقة **قبل** الكتابة، وترفض العملية كلياً إن كانت الكمية أكبر (قرار مشروع مؤكد من المستخدم، وليس افتراضاً تقنياً — **لا تُخفّفه إلى "تحذير فقط" بدون تأكيد صريح من المستخدم**).
- **i18n بدون مكتبة خارجية**: كل نص قابل للترجمة عنصر `<span class="i18n" data-ar="..." data-en="...">`, وتبديل اللغة عبر `setLang()` في كل صفحة (مكرر حالياً في 3 ملفات — مرشح جيد لإعادة الهيكلة، انظر `docs/TODO.md`).

## 5) قواعد لا يجب كسرها أبداً
1. **لا تكتب بيانات مباشرة بدون المرور بـ `appendRow_()`** — لضمان ID + Timestamp موحّدين.
2. **لا تُزل التحقق الصارم من الأرصدة** في `EntryOperations.gs` دون تأكيد صريح من المستخدم (تم اختيار "منع تماماً" وليس "تحذير فقط" بناءً على قرار موثّق).
3. **لا تُخزّن كلمات المرور كنص عادي في بيئة إنتاج فعلية** — النظام الحالي (Password عادي في شيت `Users`) مقبول فقط للنسخة الأولى/الداخلية؛ أي تحسين أمني (hashing) يجب أن يُذكر في `docs/TODO.md` وليس أن يُنفَّذ بصمت لأنه يغيّر شكل بيانات شيت `Users` الحالية (breaking change).
4. **حافظ على التوافق مع الإصدارات السابقة** (backward compatibility) لأي تعديل على أعمدة الشيتات — إضافة عمود جديد في نهاية الصف مقبولة، **حذف أو إعادة ترتيب عمود موجود يكسر كل الصفوف السابقة ويتطلب تأكيداً صريحاً**.
5. **لا تحذف أي دالة عامة (public function) مستخدمة من HTML بدون التأكد من عدم استدعائها في أي من الملفات الثلاثة** (`Login.html`, `Entry.html`, `Reports.html`) وتحديث `docs/API.md` وفق ذلك.
6. **لا تُدخل مكتبات خارجية (npm/CDN) في ملفات `.gs`** — Apps Script لا يدعم `require`/`import` من خارج المشروع؛ فقط `<script src="https://...">` مسموح داخل HTML، وهذا غير مستخدم حالياً عمداً (السرعة + العمل دون اتصال بمصادر خارجية بطيئة).

## 6) أسلوب الكود المفضّل (Coding Style)
- JavaScript بأسلوب ES6 القياسي المدعوم في V8 runtime لـ Apps Script (`const`/`let`, arrow functions, template literals, `Array.map/reduce/filter`).
- كل رقم مالي/وزني يُقرَّب بـ `Math.round(x * 1000) / 1000` (3 خانات عشرية للكغ) أو `* 100) / 100` للنسب المئوية — **حافظ على هذا التقريب الموحّد** حتى لا تختلف الأرقام المعروضة بين التقارير.
- رسائل الأخطاء والنجاح **دائماً ثنائية اللغة** بصيغة: `'نص عربي / English text'` — هذا نمط ثابت في كل `EntryOperations.gs`/`Auth.gs`، حافظ عليه في أي دالة جديدة.
- التعليقات التوضيحية أعلى كل دالة عامة (JSDoc-lite) موجودة، استمر عليها لأي دالة جديدة تُستدعى من HTML.

## 7) اعتبارات واجهة RTL العربية
- `<body dir="rtl">` هو الافتراضي؛ `setLang('en')` يبدّل `document.body.dir` إلى `'ltr'` ديناميكياً — **لا تفترض اتجاهاً ثابتاً في أي CSS جديد**، استخدم خصائص منطقية (`inset-inline-start`, `margin-inline-*`) بدلاً من `left`/`right` الصريحة (كما هو مطبّق فعلاً في `.kpi` بـ `border-inline-start`).
- الخط العربي `Cairo` والإنجليزي `Poppins` محمّلان معاً من Google Fonts؛ أي نص جديد يجب أن يكون داخل عنصر `.i18n` بسمتي `data-ar`/`data-en` وليس نصاً ثابتاً بلغة واحدة.
- الأرقام (كميات، تواريخ) تبقى بصيغة غربية (Latin numerals) في كلا اللغتين حالياً — لم يُطلب Eastern Arabic numerals، لا تُغيّر هذا بدون طلب صريح.

## 8) قيود Google Apps Script التي يجب مراعاتها

### ⚠️ ترتيب تحميل الملفات أبجدي (Alphabetical file load order)
Apps Script يدمج كل ملفات `.gs` في نطاق عام واحد، لكنه يُحمّلها ويُنفّذ أي كود على **المستوى الأعلى (top-level)** فيها بترتيب **أبجدي حسب اسم الملف** — وليس حسب ترتيب الاعتماد (dependency order). هذا يعني:
- **لا تكتب أبداً `const X = SOMETHING_FROM_ANOTHER_FILE.prop;` على المستوى الأعلى لملف**، لأن الملف الذي يحتوي `SOMETHING_FROM_ANOTHER_FILE` قد يُحمَّل لاحقاً أبجدياً فيرمي `ReferenceError`. هذا ما حدث فعلياً: `EntryOperations.gs` (E) كان يحاول قراءة `ROLES` (المعرَّف في `Setup.gs`, S) عند التحميل، و`E` تسبق `S` أبجدياً.
- **الحل الثابت في هذا المشروع**: أي قيمة تعتمد على ثابت من ملف آخر (`ROLES`, `SHEETS`, إلخ) يجب أن تُحسَب **داخل دالة (lazy)** وليس كـ `const` في أعلى الملف. مثال: `entryRoles_()` و`reportRoles_()` بدلاً من `ENTRY_ROLES`/`REPORT_ROLES` كثوابت جاهزة.
- الثوابت الآمنة على المستوى الأعلى هي فقط تلك **المستقلة تماماً عن أي ملف آخر** (مثل `SESSION_DURATION_SEC` في `Auth.gs`)، أو المُعرَّفة في `Setup.gs` نفسه (`SHEETS`, `ROLES`, `BAG_SIZE_KG`) لأنها لا تعتمد على شيء خارجي.
- عند إضافة أي ثابت جديد يعتمد على ملف آخر، اتبع نمط `entryRoles_()`/`reportRoles_()`: دالة صغيرة تُستدعى وقت التنفيذ الفعلي، لا وقت تحميل الملف.

- **لا Session حقيقية من السيرفر**: الحل الحالي (`CacheService` + توكن UUID، صلاحية 6 ساعات) هو محاكاة يدوية — أي إعادة تصميم مصادقة يجب أن يحافظ على هذا المبدأ (توكن يُرسَل من العميل مع كل نداء) لأن Apps Script HTML Service عديم الحالة (stateless).
- **حد وقت التنفيذ**: 6 دقائق لكل تنفيذ (Consumer) — دوال القراءة الحالية (`readSheetAsObjects_`) تقرأ الشيت كاملاً بنداء واحد (`getRange().getValues()`) عمداً لتفادي نداءات متكررة بطيئة؛ **لا تستبدلها بحلقة `for` تقرأ خلية خلية**.
- **`CacheService` له حد حجم للقيمة المخزَّنة (~100KB)** — الجلسة الحالية تخزّن فقط username/role/fullName، لا تُضخّم هذا الكائن.
- **لا قاعدة بيانات علائقية حقيقية**: لا مفاتيح خارجية (Foreign Keys) ولا Transactions — الاتساق بين الشيتات مسؤولية الكود (التحقق الصارم في `EntryOperations.gs`) وليس مضموناً من المنصة. أي عملية تكتب لأكثر من شيت في نفس الوقت (لا يوجد حالياً) يجب أن تتحمّل احتمال فشل جزئي.
- **حد حجم ملف HTML/Script**: `LogoBase64.html` (~35KB نص) مقبول ضمن الحدود؛ لا تُضف صوراً كبيرة بنفس الطريقة (Base64) — استخدم Google Drive + `DriveApp` مع رابط عام إن احتجت صوراً إضافية أكبر.

## 8.1) SPREADSHEET_ID — قاعدة إلزامية لأي بيئة clasp
- `getSS_()` في `SheetService.gs` هو **المكان الوحيد** الذي يقرر أي Spreadsheet يُستخدم. لا تستدعِ `SpreadsheetApp.getActiveSpreadsheet()` أو `SpreadsheetApp.openById()` من أي ملف آخر — استخدم `getSS_()` دائماً.
- `SPREADSHEET_ID` في `Setup.gs` هو مصدر الحقيقة الوحيد لمعرّف الشيت. لا تُثبّت (hardcode) معرّف شيت في أي مكان آخر بالكود.
- عند تشغيل `initializeSpreadsheet()` أول مرة على مشروع clasp مستقل بدون تعيين `SPREADSHEET_ID`، ستحصل على خطأ واضح ثنائي اللغة بدل فشل صامت — حافظ على هذا السلوك (fail loudly) عند أي تعديل مستقبلي على `getSS_()`.

## 8.2) رابط /exec الحقيقي — لا تعتمد أبداً على window.location في العميل
واجهة Apps Script HTML Service تُعرض للمستخدم داخل **إطار معزول (sandboxed iframe)** على نطاق `googleusercontent.com`، مختلف تماماً عن نطاق `/exec` الظاهر في شريط العنوان. النتيجة:
- `window.location.href` من داخل أي صفحة `.html` **يعطي رابط الإطار الداخلي**، وليس رابط `/exec` الحقيقي.
- **لا تحاول أبداً بناء رابط التنقّل (`window.top.location.href = ...`) من `window.location` في العميل** — هذا يسبب انتقال المتصفح فعلياً إلى رابط الإطار الداخلي مباشرة، مما ينتج **صفحة بيضاء فارغة** (تم تشخيص هذا فعلياً وإصلاحه في هذا المشروع).
- **الحل الثابت المعتمد**: الخادم (`Code.gs` → `doGet`) يحقن الرابط الصحيح عبر `ScriptApp.getService().getUrl()` في متغيّر `template.baseUrl`، وكل صفحة HTML تقرأه كـ:
  ```js
  const BASE_URL = '<?!= baseUrl ?>';
  ```
  ثم تستخدم `BASE_URL` حصراً لكل تنقّل (`window.top.location.href = BASE_URL + '?page=...'`).
- أي صفحة `.html` جديدة تحتاج التنقّل بين الصفحات **يجب** أن تتبع نفس النمط (`template.xxx = ...` في `Code.gs` + `<?!= xxx ?>` في الصفحة)، لا تُعِد اختراع طريقة قراءة الرابط من طرف العميل.

## 9) افتراضات هيكل الشيت (راجع docs/Database.md للتفاصيل الكاملة)
- الصف 1 في كل شيت = رؤوس أعمدة (Headers)، ولا يجوز حذفه أو تغيير ترتيبه.
- عمود `ID` دائماً أول عمود، وعمود `Timestamp` دائماً آخر عمود — أي عمود جديد يُضاف **بينهما**، وليس بعد `Timestamp`.
- `Config` هو مصدر الحقيقة الوحيد لحجم الكيس (`BagSizeKg`) وأصناف القهوة (`CoffeeType1/2`) — **لا تُثبّت (hardcode) هذه القيم في أي دالة**؛ استخدم `getConfigMap_()`.

## 10) أفضل الممارسات عند تعديل المشروع
- أي تعديل على شكل بيانات شيت موجود (إضافة/حذف عمود) ⇒ حدّث `docs/Database.md` وأضف سطراً في `CHANGELOG.md` تحت `Changed`.
- أي دالة عامة جديدة تُستدعى من HTML ⇒ وثّقها في `docs/API.md` بنفس القالب المستخدم حالياً (Purpose / Parameters / Return / Errors / Called from).
- **حافظ دوماً على التوافق العكسي** ما لم يُطلب خلاف ذلك صراحة، و**لا تحذف وظيفة قائمة دون تأكيد صريح من المستخدم** — هذه قاعدة صارمة غير قابلة للتفاوض في هذا المشروع.
- قبل أي Refactor كبير (مثل توحيد `setLang()` في ملف واحد مشترك)، تأكد من عدم كسر أي من الصفحات الثلاث، واختبر يدوياً بعد `clasp push` + `New deployment`.
