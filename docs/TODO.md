# TODO — خارطة الطريق

مرتّبة حسب الأولوية. هذا الملف يُحدَّث مع كل تطوير مستقبلي — أضف/اشطب البنود بدلاً من حذفها بصمت (راجع `CLAUDE.md` §10).

## 🔴 Critical (حرجة — قبل أي استخدام إنتاجي حقيقي)
- [ ] **تشفير كلمات المرور**: `Users.Password` مخزَّنة حالياً كنص عادي. يجب استخدام `Utilities.computeDigest` (SHA-256 + salt) قبل أي استخدام يتجاوز فريقاً داخلياً صغيراً موثوقاً. **تغيير كاسر (breaking)** — يتطلب هجرة بيانات المستخدمين الحاليين.
- [ ] **صلاحيات مشاركة Google Sheet نفسه**: حالياً أي شخص لديه صلاحية "Editor" على ملف Sheet يمكنه تعديل البيانات مباشرة متجاوزاً كل تحقق `EntryOperations.gs`. يجب تقييد مشاركة الـ Sheet لحساب الخدمة (service account) أو المطورين فقط، والاعتماد على الـ Web App كطريقة الوصول الوحيدة للمستخدمين العاديين.
- [ ] **نسخ احتياطي دوري**: لا توجد آلية نسخ احتياطي تلقائي حالياً — يُنصح بإضافة `Trigger` زمني يُصدّر نسخة يومية (Google Drive → مجلد نسخ احتياطي).

## 🟠 Security (أمان)
- [ ] Rate limiting على `login()` لمنع محاولات تخمين كلمة المرور المتكررة (لا يوجد حالياً أي حد لعدد المحاولات).
- [ ] تسجيل محاولات الدخول الفاشلة في `AuditLog` (حالياً يُسجَّل النجاح فقط).
- [ ] إخفاء تفاصيل الخطأ التقنية عن المستخدم النهائي في بيئة الإنتاج (حالياً `err.message` يُعرض كما هو في `withFailureHandler`).

## 🟡 Refactoring (إعادة هيكلة)
- [ ] توحيد كود `setLang()` المكرر في 3 ملفات (`Login.html`, `Entry.html`, `Reports.html`) في ملف `I18n.html` مشترك يُضمَّن بـ `include()`.
- [ ] توحيد منطق `getBaseUrl_()` وزر الخروج (`doLogout`) في نفس الملف المشترك أعلاه.
- [ ] استخراج CSS الخاص بـ `.login-card` من `Style.html` العام إلى نطاق أضيق إذا كبر الملف أكثر (حالياً لا يزال مقبول الحجم).

## 🟢 Performance (أداء)
- [ ] **Caching للتقارير**: `getAllReports` يعيد قراءة كل الشيتات من الصفر في كل مرة (لا تخزين مؤقت). مع نمو البيانات (آلاف الصفوف)، يُفضَّل تخزين النتائج المجمّعة في `CacheService` لمدة قصيرة (1-5 دقائق) مع زر "تحديث" لإجبار إعادة الحساب.
- [ ] **Pagination للجداول**: جداول `Reports.html` تعرض كل الصفوف دفعة واحدة (scroll داخلي فقط). مع نمو البيانات، أضف Pagination أو تحميل تدريجي.

## 🔵 UX (تجربة المستخدم)
- [ ] تنبيه بصري (وليس رفض) عند اقتراب نسبة الهدر من `MaxRoastingWastePercent`/`MaxPackingWastePercent` المحددة في `Config` — القيم موجودة في الشيت لكنها غير مُفعَّلة فعلياً في منطق `EntryOperations.gs` حالياً.
- [ ] تأكيد قبل الحفظ (Confirm dialog) عند إدخال كمية هدر مرتفعة بشكل غير معتاد.
- [ ] إظهار سجل آخر 5 عمليات مباشرة أسفل كل نموذج إدخال (تغذية راجعة فورية دون الحاجة لفتح صفحة التقارير).
- [ ] وضع "طباعة" مخصص لصفحة التقارير (`@media print`) لتصدير PDF عبر متصفح مباشرة.
- [ ] رسالة تحميل أوضح (Skeleton loading) بدل النص البسيط الحالي أثناء `refreshAll()`.

## 🟣 Future Features (ميزات مستقبلية)
- [ ] **دعم عدد ديناميكي من أصناف القهوة** بدلاً من صنفين ثابتين (`CoffeeType1`/`CoffeeType2`) — يتطلب تحويل `Config` لتخزين قائمة أصناف قابلة للتوسعة، وتعديل كل القوائم المنسدلة والتقارير لتكرار ديناميكي.
- [ ] **دعم نسب الخلط بين الأصناف** عند الإرسال للتحميص (تم استبعادها من النسخة الأولى بقرار صريح — إن احتاج العمل لاحقاً لتتبّع "خلطة X% Arabica + Y% Robusta" كوصفة موثّقة لكل دفعة).
- [ ] واجهة إدارة مستخدمين داخل التطبيق (حالياً تُدار يدوياً من شيت `Users` مباشرة) — تحتاج دور `Admin` فقط.
- [ ] واجهة لعرض/تعديل `Config` من داخل التطبيق بدلاً من تعديل الشيت يدوياً.
- [ ] تصدير أي تقرير إلى Excel/PDF مباشرة من الواجهة (`SpreadsheetApp` يدعم التصدير برمجياً).
- [ ] إشعارات بريد إلكتروني تلقائية (`MailApp`) عند تجاوز نسبة هدر معينة حداً معيّناً.
- [ ] رسم بياني (Chart) لاتجاه نسبة الهدر عبر الزمن في صفحة التقارير.
- [ ] دعم أوزان أكياس متعددة (ليس 200غ فقط) إن تنوّع خط الإنتاج مستقبلاً.
